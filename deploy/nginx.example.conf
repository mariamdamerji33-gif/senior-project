# Example: nginx terminates TLS and serves the SPA + proxies /api to Node.
# Replace server_name, paths, and upstream with your values.
# Get certificates with certbot (Let's Encrypt) or your host's TLS.

# upstream api_backend {
#   server 127.0.0.1:5000;
# }

# server {
#   listen 443 ssl http2;
#   server_name app.yourschool.org;
#
#   ssl_certificate     /etc/letsencrypt/live/app.yourschool.org/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/app.yourschool.org/privkey.pem;
#
#   root /var/www/autism-school-platform/dist;
#   index index.html;
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
#
#   location /api {
#     proxy_pass http://api_backend;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }

# Alternative: API on api.yourschool.org (separate server block), SPA on app.yourschool.org
# — set VITE_API_BASE_URL=https://api.yourschool.org in frontend/.env.production
