# Deploy folder (not MVC application code)

`deploy/` contains hosting notes and config examples (Vercel, Netlify, etc.). Deployment assets are **not** MVC layers. Application MVC lives in `backend/`, `frontend/src/mvc/`, and `mobile/src/mvc/`.
