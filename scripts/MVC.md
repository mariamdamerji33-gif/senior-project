# Scripts folder (not MVC application code)

`scripts/` holds one-off automation (deploy checks, data tasks). It is **not** part of the product MVC stack. Use `backend/`, `frontend/src/mvc/`, and `mobile/src/mvc/` for application architecture.
